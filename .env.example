# Database connection URL (e.g. for Neon DB, append ?sslmode=require)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres?sslmode=disable

# Server Configuration
PORT=3000
HOST=127.0.0.1
